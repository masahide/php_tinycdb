// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("tinycdb", "for tinycdb support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("tinycdb", "enable tinycdb support", "no");

if (PHP_TINYCDB != "no") {
	EXTENSION("tinycdb", "tinycdb.c");
}

